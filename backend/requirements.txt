# Removed: bcrypt<4.0.0 (let passlib[bcrypt] pull a compatible bcrypt; older pins break on py3.13)
pydantic
email-validator
fastapi
uvicorn[standard]
python-multipart
alembic
requests
sqlalchemy
python-dotenv
sqlalchemy[asyncio]
asyncpg
websockets
python-jose[cryptography]
passlib[bcrypt]
# pin bcrypt to v3.x to ensure compatibility with passlib which expects the older
# bcrypt package metadata (some bcrypt v4+ builds changed internals causing
# passlib to fall back with a warning). Use <4.0.0 to be safe.
bcrypt<4.0.0
aiosmtplib
httpx
itsdangerous
celery[redis]
redis
# Use psycopg2-binary for <3.13; use psycopg (v3) binary wheels for >=3.13
psycopg2-binary; python_version < "3.13"
psycopg[binary]; python_version >= "3.13"
pydantic-settings
aiofiles
azure-storage-blob
azure-identity
python-magic